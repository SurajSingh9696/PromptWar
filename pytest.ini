[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
# Register custom markers
markers =
    unit: Pure unit tests (no GCP credentials required)
    integration: Tests that require live service integration
    slow: Tests that call external network services
